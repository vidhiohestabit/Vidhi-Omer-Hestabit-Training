# Code Execution Report

**Generated:** 2026-04-21 14:54:45
**Task:** give me the code for hash map

---

## Generated Code

```python
class Node:
    """A node in the hash map."""
    
    def __init__(self, key, value, isKey=True):
        """
        Initialize a Node.

        Args:
        key (str): The key of the node.
        value (str): The value of the node.
        isKey (bool): Whether the node's data is a key. Defaults to True.
        """
        self.key = key
        self.value = value
        self.isKey = isKey
        self.next = None

class HashMap:
    """
    A hash map, implemented as a dynamic array where each element is a linked list.
    
    Attributes:
    capacity (int): The initial capacity of the hash map.
    size (int): The number of elements in the hash map.
    table (list): The hash map's table, implemented as a dynamic array of linked lists.
    """

    def __init__(self, default_capacity=16, default_load_factor=0.75):
        """
        Initialize a HashMap.

        Args:
        default_capacity (int, optional): The initial capacity of the hash map. Defaults to 16.
        default_load_factor (float, optional): The load factor of the hash map. Defaults to 0.75.
        """
        self.capacity = default_capacity
        self.size = 0
        self.table = [[] for _ in range(default_capacity)]

    def _hash(self, key):
        """
        Calculate the hash code of a key.

        Args:
        key (str): The key to calculate the hash code for.

        Returns:
        int: The hash code of the key.
        """
        return sum(ord(c) for c in key) % self.capacity

    def put(self, key, value):
        """
        Add or update a key-value pair in the hash map.

        Args:
        key (str): The key of the pair.
        value (str): The value of the pair.

        Returns:
        str: The previous value of the pair if it existed, otherwise None.
        """
        index = self._hash(key)
        node = next((i for i in self.table[index] if i.key == key), None)
        if node:
            # If the node exists, update its value.
            node.value = value
        else:
            # If the node does not exist, add a new node to the list.
            self.table[index].append(Node(key, value))
        self.size += 1
        return value if node is None else node.value

    def get(self, key):
        """
        Get the value of a key in the hash map.

        Args:
        key (str): The key to get the value for.

        Returns:
        str: The value of the key if it exists, otherwise None.
        """
        index = self._hash(key)
        node = next((i for i in self.table[index] if i.key == key), None)
        return node.value if node else None

    def resize(self):
        """
        Resize the hash table to its current size.
        """
        new_capacity = self.capacity * 2
        new_table = [[] for _ in range(new_capacity)]
        for bucket in self.table:
            for node in bucket:
                new_index = node.key % new_capacity
                new_table[new_index].append(node)
        self.capacity = new_capacity
        self.table = new_table

    def load_factor(self):
        """
        Calculate the load factor of the hash map.

        Returns:
        float: The load factor, calculated as (size / capacity).
        """
        return self.size / self.capacity

    def __len__(self):
        """
        Get the number of elements in the hash map.

        Returns:
        int: The number of elements in the hash map.
        """
        return self.size
```

---

## Execution Output

```
✅ Code executed successfully (no output)
```

---

## Files

| File | Purpose |
|------|---------|
| `outputs/code_20260421_145445.py` | Raw Python source |
| `outputs/code_20260421_145445.md` | This report |
