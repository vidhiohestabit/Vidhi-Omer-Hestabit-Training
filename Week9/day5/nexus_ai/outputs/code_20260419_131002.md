# Code Execution Report

**Generated:** 2026-04-19 13:10:02
**Task:** write a binary search code

---

## Generated Code

```python
# Function to perform a binary search on a sorted array
def binary_search(arr, target):
    """
    Performs a binary search on a sorted array to find the target value.

    Args:
    arr (list): A sorted list of items.
    target: The value to search for.

    Returns:
    int: The index of the target value if found, -1 otherwise.
    """
    # Initialize the search range to the entire array
    low = 0  # Start from the beginning of the array
    high = len(arr) - 1  # End at the last index of the array

    # Continue searching as long as the search range is not empty
    while low <= high:
        # Calculate the middle index of the current range
        mid = (low + high) // 2

        # Check if the target value is at the middle index
        if arr[mid] == target:
            # Return the index of the target value if found
            return mid
        # If the target value is less than the value at the middle index,
        # search the left half
        elif arr[mid] > target:
            # Update the upper bound of the search range
            high = mid - 1
        # If the target value is greater than the value at the middle index,
        # search the right half
        else:
            # Update the lower bound of the search range
            low = mid + 1

    # Return -1 to indicate that the target value is not in the array
    return -1

# Example usage:
arr = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91]
target = 23
index = binary_search(arr, target)

if index != -1:
    # Print the result if the target value is found
    print(f"Target value {target} found at index {index}.")
else:
    # Print the result if the target value is not found
    print(f"Target value {target} not found.")

# Additional example usage:
arr = [4, 10, 15, 22, 28, 35, 42, 50, 58, 65]
target = 28
index = binary_search(arr, target)

if index != -1:
    # Print the result if the target value is found
    print(f"Target value {target} found at index {index}.")
else:
    # Print the result if the target value is not found
    print(f"Target value {target} not found.")
```

---

## Execution Output

```
Target value 23 found at index 5.
Target value 28 found at index 4.
```

---

## Files

| File | Purpose |
|------|---------|
| `outputs/code_20260419_131002.py` | Raw Python source |
| `outputs/code_20260419_131002.md` | This report |
